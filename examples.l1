/*
 * examples.l1
 */
include stdio;

void
writeln(byte @str):
{
        write(str);
        write("\n");
}

byte /* add x and y */
add(byte x: 0, byte y: 0):
        ret x + y;

byte /* Entry point */
main():
{
        writeln("Hello, World!");
        ret add(40, 2);
}